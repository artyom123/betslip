const GAMES_ACTION_TYPES = Object.freeze({
    SET_GAMES: 'set games',
});

const USER_ACTION_TYPES = Object.freeze({
    CHANGE_MONEY: 'change money',
    SET_COEFFICIENTS: 'set coefficients',
});

const INIT = '__INIT__';

export {
    GAMES_ACTION_TYPES,
    USER_ACTION_TYPES,
    INIT,
};
